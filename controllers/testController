const connection = require('../dbConfig');

const testCtrl = {
  getText: async (req, res) => {
    connection.query('select * from TB_TEST', (error, rows) => {
      if (error) throw error;
      res.send(rows);
    });
  },
};

module.exports = {
  testCtrl,
};
