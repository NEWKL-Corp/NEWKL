const connection = require('../dbConfig');

const testCtrl = {
  getText: async (req, res) => {
    connection.query('select * from test_table', (error, rows) => {
      if (error) throw error;
      res.send(rows);
    });
  },
};

module.exports = {
  testCtrl,
};
