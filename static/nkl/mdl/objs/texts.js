import { WebGLRenderer, PCFSoftShadowMap, sRGBEncoding } from 'three';

const mTxt = function (_t, _v) {
  return new mTxt.fn.init(_t, _v);
};

mTxt.fn = mTxt.prototype = {
  constructor: mTxt,

  ver: '22-0929-1541',
  length: 0,
  renderer: renderer,

  load: () => {

  }
}

const init = mTxt.fn.init = function (_t, _v) {
  if (!_t) { return this; }

  let _r;
  if (typeof _t === "string") { ///. 문자열을 나타낸다

  } else { ///. 배열을 나타낸다

  }

  return _r;
}

init.prototype = mTxt.fn;

export { mTxt};
